modules:
  http_2xx:
    prober: http
    timeout: 5s
    http:
      valid_http_versions: ["HTTP/1.1", "HTTP/2.0"]
      valid_status_codes: []   # Kosong = hanya 2xx yang dianggap sukses
      method: GET
      follow_redirects: true
      preferred_ip_protocol: "ip4"
